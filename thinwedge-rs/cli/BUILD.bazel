load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "thinwedge_rust_crate")
load("//bazel/platforms:release_binaries.bzl", "multiplatform_binaries")

thinwedge_rust_crate(
    name = "cli",
    crate_name = "thinwedge_cli",
    extra_binaries = [
        "//thinwedge-rs/bwrap:bwrap",
    ],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
)

multiplatform_binaries(
    name = "thinwedge",
)
