[package]
name = "thinwedge-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single ThinWedge workspace dependency.
# Add new ThinWedge surface area to `thinwedge-core-api` instead of depending on
# additional `thinwedge-*` crates here.
thinwedge-core-api = { workspace = true }
tracing = { workspace = true }
