load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "thinwedge_rust_crate")

thinwedge_rust_crate(
    name = "tui",
    crate_name = "thinwedge_tui",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ) + [
        "//thinwedge-rs/collaboration-mode-templates:templates/default.md",
        "//thinwedge-rs/collaboration-mode-templates:templates/plan.md",
    ],
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
    ]),
    integration_compile_data_extra = ["src/test_backend.rs"],
    extra_binaries = [
        "//thinwedge-rs/cli:thinwedge",
    ],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
)
