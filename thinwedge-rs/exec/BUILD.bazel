load("//:defs.bzl", "thinwedge_rust_crate")

thinwedge_rust_crate(
    name = "exec",
    crate_name = "thinwedge_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
