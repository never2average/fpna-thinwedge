load("//:defs.bzl", "thinwedge_rust_crate")

thinwedge_rust_crate(
    name = "features",
    crate_name = "thinwedge_features",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
