load("//:defs.bzl", "thinwedge_rust_crate")

thinwedge_rust_crate(
    name = "plugin",
    crate_name = "thinwedge_plugin",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
