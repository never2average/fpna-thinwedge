# Upstream Codex Weekly Sync

This repository tracks the May 2026 upstream Codex changes that are relevant to ThinWedge.

| Upstream change                    | Commit                                                                             | ThinWedge status                                                                                                                                                                                                                                      |
| ---------------------------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vim composer mode                  | `b6f81257f8`                                                                       | Tracked for a dedicated TUI PR. The upstream patch touches the composer, textarea, keymap setup, snapshots, and render model, and does not apply cleanly to ThinWedge's already-diverged composer stack.                                              |
| Raw scrollback mode                | `5e0a4adbe5`                                                                       | Tracked for a dedicated TUI PR. ThinWedge already has normal-scrollback/alternate-screen controls, so this needs a local UX merge rather than a blind cherry-pick.                                                                                    |
| Linux sandbox/bubblewrap packaging | `26f355b67b`, `3ec18a2c0a`, `a736cb55a2`, `9766d3d51c`, `22326e263c`, `123ec8b035` | Partially ported here by packaging the Linux sandbox helper in Linux npm release payloads. ThinWedge still avoids default vendored LGPL bubblewrap embedding for public-release readiness.                                                            |
| Memory MCP dot-path hardening      | `de924af134`                                                                       | Partially ported as `is_public_memory_path` in the ThinWedge memory read crate. ThinWedge does not have upstream's `memories/mcp` crate, so enforcement in a read/list/search surface remains a follow-up before this should be called fully shipped. |
| Compact lifecycle hooks            | `527d52df03`                                                                       | Ported into ThinWedge hooks/core with `PreCompact` and `PostCompact` event dispatch around local and remote compaction.                                                                                                                               |

The TUI changes are intentionally left as separate follow-ups because both upstream patches span more than a thousand lines of interactive UI behavior and snapshot churn. Keeping them out of this PR avoids shipping half-wired keybindings or scrollback state.
